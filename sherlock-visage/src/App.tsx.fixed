import * as React from 'react';
import { Row, Col, Card, Statistic, Progress, Typography, Alert } from 'antd';
import { 
  DashboardOutlined, 
  DollarOutlined, 
  DatabaseOutlined,
  CloudOutlined,
  TeamOutlined,
  RocketOutlined,
  EyeOutlined,
  UserOutlined
} from '@ant-design/icons';
import NairobiFloorPlan from './components/visualization/NairobiFloorPlan';
import { AgentAvatarGrid } from './components/avatars';

const { Title, Paragraph } = Typography;

const App: React.FC = () => {
  // Mock data
  const delegates = [
    { id: 'strategic_architect', name: 'Strategic Architect', type: 'strategist' as const, status: 'active' as const, model: 'DeepSeek-R1', cost: 2.0, cpuUsage: 45.2, memoryUsage: 67.8, tokenUsage: 1250, lastActive: '2026-04-07T10:30:00Z', coordinates: { x: 150, y: 100 }, color: '#4CAF50', glowIntensity: 0.8 },
    { id: 'lead_developer', name: 'Lead Developer', type: 'executor' as const, status: 'active' as const, model: 'DeepSeek-V3.2', cost: 0.3, cpuUsage: 78.5, memoryUsage: 82.3, tokenUsage: 890, lastActive: '2026-04-07T10:25:00Z', coordinates: { x: 350, y: 200 }, color: '#2196F3', glowIntensity: 0.9 },
    { id: 'security_auditor', name: 'Security Auditor', type: 'validator' as const, status: 'idle' as const, model: 'GPT-4o', cost: 1.0, cpuUsage: 12.3, memoryUsage: 34.5, tokenUsage: 210, lastActive: '2026-04-07T09:45:00Z', coordinates: { x: 550, y: 100 }, color: '#FF9800', glowIntensity: 0.3 },
    { id: 'rapid_prototyper', name: 'Rapid Prototyper', type: 'executor' as const, status: 'active' as const, model: 'grok-4-1-fast-non-reasoning', cost: 0.1, cpuUsage: 65.7, memoryUsage: 71.2, tokenUsage: 540, lastActive: '2026-04-07T10:15:00Z', coordinates: { x: 200, y: 300 }, color: '#9C27B0', glowIntensity: 0.7 },
    { id: 'algorithm_specialist', name: 'Algorithm Specialist', type: 'analyst' as const, status: 'idle' as const, model: 'Kimi-K2.5', cost: 1.5, cpuUsage: 95.8, memoryUsage: 88.9, tokenUsage: 1250, lastActive: '2026-04-07T09:30:00Z', coordinates: { x: 500, y: 300 }, color: '#F44336', glowIntensity: 1.0 },
    { id: 'crisis_resolver', name: 'Crisis Resolver', type: 'monitor' as const, status: 'error' as const, model: 'DeepSeek-R1', cost: 3.0, cpuUsage: 5.2, memoryUsage: 18.7, tokenUsage: 75, lastActive: '2026-04-06T16:20:00Z', coordinates: { x: 350, y: 400 }, color: '#607D8B', glowIntensity: 0.1 },
    { id: 'documentation_specialist', name: 'Documentation Specialist', type: 'archivist' as const, status: 'active' as const, model: 'text-embedding-3-small', cost: 0.05, cpuUsage: 32.4, memoryUsage: 45.6, tokenUsage: 320, lastActive: '2026-04-07T10:10:00Z', coordinates: { x: 650, y: 400 }, color: '#795548', glowIntensity: 0.6 },
  ];

  const systemMetrics = {
    health: 80,
    diskUsage: 68,
    monthlyCost: 425,
    tokenEfficiency: 72,
    contextHealth: 88,
    uptime: '3 minutes'
  };

  const getStatusColor = (status: string) => {
    switch(status) {
      case 'active': return '#52c41a';
      case 'idle': return '#fa8c16';
      case 'error': return '#f5222d';
      default: return '#8c8c8c';
    }
  };

  return (
    <div style={{ padding: 24, minHeight: '100vh', backgroundColor: '#f0f2f5' }}>
      <Title level={2}>
        <DashboardOutlined style={{ marginRight: 12 }} />
        Sherlock Visage - Operations Command Dashboard
      </Title>
      <Paragraph type="secondary">
        Nairobi HQ • 7 AI Delegates • Real-time Monitoring • Cost Optimization
      </Paragraph>

      {/* System Overview */}
      <Row gutter={[16, 16]} style={{ marginBottom: 24 }}>
        <Col span={6}>
          <Card>
            <Statistic
              title="System Health"
              value={systemMetrics.health}
              suffix="%"
              prefix={<CloudOutlined />}
              valueStyle={{ color: systemMetrics.health > 70 ? '#3f8600' : '#cf1322' }}
            />
            <Progress percent={systemMetrics.health} size="small" status="active" />
          </Card>
        </Col>
        <Col span={6}>
          <Card>
            <Statistic
              title="Monthly Cost"
              value={systemMetrics.monthlyCost}
              prefix={<DollarOutlined />}
              suffix="USD"
              valueStyle={{ color: '#1890ff' }}
            />
            <div style={{ fontSize: 12, color: '#8c8c8c' }}>
              Budget: $280 - $570
            </div>
          </Card>
        </Col>
        <Col span={6}>
          <Card>
            <Statistic
              title="Disk Usage"
              value={systemMetrics.diskUsage}
              suffix="%"
              prefix={<DatabaseOutlined />}
              valueStyle={{ color: systemMetrics.diskUsage > 80 ? '#fa8c16' : '#52c41a' }}
            />
            <div style={{ fontSize: 12, color: '#8c8c8c' }}>
              9.1GB available
            </div>
          </Card>
        </Col>
        <Col span={6}>
          <Card>
            <Statistic
              title="Token Efficiency"
              value={systemMetrics.tokenEfficiency}
              suffix="%"
              prefix={<RocketOutlined />}
              valueStyle={{ color: '#722ed1' }}
            />
            <div style={{ fontSize: 12, color: '#8c8c8c' }}>
              Delta vs Full: 72% savings
            </div>
          </Card>
        </Col>
      </Row>

      {/* AI Agent Avatars */}
      <AgentAvatarGrid 
        delegates={delegates}
        onAgentClick={(agentId) => {
          console.log(`Agent clicked: ${agentId}`);
          // Add your click handler logic here
        }}
        size="medium"
        showStatus={true}
      />

{/* Virtual Office 3D Layout */}
      <Card 
        title={
          <span>
            <EyeOutlined style={{ marginRight: 8 }} />
            Virtual Office - Nairobi HQ
          </span>
        }
        style={{ marginBottom: 24 }}
      >
        <Alert
          message="Interactive 3D Command Center"
          description="Isometric glass terminals with parallax scrolling • 7 agent stations • Real-time monitoring • Click to focus"
          type="info"
          showIcon
          style={{ marginBottom: 16 }}
        />
        <NairobiFloorPlan />
      </Card>

      {/* Efficiency Guards Status */}
      <Card title="Efficiency Guards Configuration" style={{ marginBottom: 24 }}>
        <Alert
          message="Efficiency Guards ACTIVE"
          description="Delta-Only Mode • Tiered Memory • Cost Gatekeeping • High-Cost Alert • Context Compression"
          type="success"
          showIcon
          style={{ marginBottom: 16 }}
        />
        
        <Row gutter={[16, 16]}>
          <Col span={12}>
            <div style={{ padding: 12, backgroundColor: '#f6ffed', borderRadius: 8 }}>
              <div style={{ fontWeight: 500, marginBottom: 4 }}>Cost Optimization</div>
              <div style={{ fontSize: 12, color: '#8c8c8c' }}>
                • Default research: grok-4-1-fast-non-reasoning (0.1x cost)<br/>
                • Security override: GPT-4o for scores &gt;7.0<br/>
                • 7% cost savings vs naive routing
              </div>
            </div>
          </Col>
          <Col span={12}>
            <div style={{ padding: 12, backgroundColor: '#e6f7ff', borderRadius: 8 }}>
              <div style={{ fontWeight: 500, marginBottom: 4 }}>Performance</div>
              <div style={{ fontSize: 12, color: '#8c8c8c' }}>
                • Compression: Every 5 turns<br/>
                • Health score: 80% target<br/>
                • File descriptors: 65,536 (fixed)<br/>
                • Disk space: 9.1GB available
              </div>
            </div>
          </Col>
        </Row>
      </Card>

      {/* Access Information */}
      <Alert
        message="Production Dashboard"
        description={
          <div>
            <div>✅ Nginx serving on port 80</div>
            <div>✅ No SSH tunneling required</div>
            <div>✅ Direct browser access: <strong>http://10.0.0.4</strong></div>
            <div>✅ Backend API: http://10.0.0.4/api/</div>
            <div>✅ WebSocket: ws://10.0.0.4/ws</div>
          </div>
        }
        type="info"
        showIcon
      />

      <div style={{ marginTop: 24, textAlign: 'center', color: '#8c8c8c', fontSize: 12 }}>
        Sherlock Visage Dashboard • Nairobi HQ Operations • Shavi Software Limited
      </div>
    </div>
  );
};

export default App;
